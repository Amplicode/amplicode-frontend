const sortBySelectorOptions: DefaultOptionType[] = [
  <% orderByArguments.forEach(orderBy => { -%>
    {
      label: <><%= splitByCapitalLetter(fatSnakeToPascal(orderBy)) -%> (<ArrowDownOutlined />)</>,
      value: JSON.stringify({
        direction: Direction.Desc,
        property: OwnerOrderByProperty.<%= fatSnakeToPascal(orderBy) -%>
      })
    },
    {
      label: <><%= splitByCapitalLetter(fatSnakeToPascal(orderBy)) -%> (<ArrowUpOutlined />)</>,
      value: JSON.stringify({
        direction: Direction.Asc,
        property: OwnerOrderByProperty.<%= fatSnakeToPascal(orderBy) -%>
      })
    },
  <% }) -%>
];
